
// filter a list with multiple keywords
// any list entry containing any keyword remains in the list

export const filterByKeywords = (list: string[], keywords: string[]): string[] => {
  return []
}
